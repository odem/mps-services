# This file is auto-generated by the Mailu configuration wizard.
# Please read the documentation before attempting any change.
# Generated for compose flavor

version: "2.2"

services:
  # External dependencies
  redis:
    image: redis:alpine
    restart: always
    volumes:
      - "$DATA_FOLDER/redis:/data"
    depends_on:
      - resolver
    dns:
      - 192.168.203.254

  # Core services
  front:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}nginx:${MAILU_VERSION:-1.9}
    restart: always
    env_file: .env
    logging:
      driver: json-file
    ports:
      - "194.163.136.152:13080:80"
      - "194.163.136.152:13443:443"
      - "194.163.136.152:25:25"
      - "194.163.136.152:465:465"
      - "194.163.136.152:587:587"
      - "194.163.136.152:110:110"
      - "194.163.136.152:995:995"
      - "194.163.136.152:143:143"
      - "194.163.136.152:993:993"
    volumes:
      - /srv/docker/swag/etc/letsencrypt/live/$DOMAIN/fullchain.pem:/certs/fullchain.pem:ro
      - /srv/docker/swag/etc/letsencrypt/live/$DOMAIN/privkey.pem:/certs/privkey.pem:ro
      - "$DATA_FOLDER/overrides/nginx:/overrides:ro"
    depends_on:
      - resolver
    dns:
      - 192.168.203.254

  resolver:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}unbound:${MAILU_VERSION:-1.9}
    env_file: .env
    restart: always
    networks:
      default:
        ipv4_address: 192.168.203.254

  admin:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}admin:${MAILU_VERSION:-1.9}
    restart: always
    env_file: .env
    volumes:
      - "$DATA_FOLDER/data:/data"
      - "$DATA_FOLDER/dkim:/dkim"
    depends_on:
      - redis
      - resolver
    dns:
      - 192.168.203.254

  imap:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}dovecot:${MAILU_VERSION:-1.9}
    restart: always
    env_file: .env
    volumes:
      - "$DATA_FOLDER/mail:/mail"
      - "$DATA_FOLDER/overrides/dovecot:/overrides:ro"
    depends_on:
      - front
      - resolver
    dns:
      - 192.168.203.254

  smtp:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}postfix:${MAILU_VERSION:-1.9}
    restart: always
    env_file: .env
    volumes:
      - "$DATA_FOLDER/mailqueue:/queue"
      - "$DATA_FOLDER/overrides/postfix:/overrides:ro"
    depends_on:
      - front
      - resolver
    dns:
      - 192.168.203.254

  antispam:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}rspamd:${MAILU_VERSION:-1.9}
    hostname: antispam
    restart: always
    env_file: .env
    volumes:
      - "$DATA_FOLDER/filter:/var/lib/rspamd"
      - "$DATA_FOLDER/overrides/rspamd:/etc/rspamd/override.d:ro"
    depends_on:
      - front
      - resolver
    dns:
      - 192.168.203.254

  # Optional services

  fetchmail:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}fetchmail:${MAILU_VERSION:-1.9}
    restart: always
    env_file: .env
    volumes:
      - "$DATA_FOLDER/data/fetchmail:/data"
    depends_on:
      - resolver
    dns:
      - 192.168.203.254

  # Webmail
  webmail:
    image: ${DOCKER_ORG:-mailu}/${DOCKER_PREFIX:-}rainloop:${MAILU_VERSION:-1.9}
    restart: always
    env_file: .env
    volumes:
      - "$DATA_FOLDER/webmail:/data"
      - "$DATA_FOLDER/overrides/rainloop:/overrides:ro"
    depends_on:
      - imap
      - resolver
    dns:
      - 192.168.203.254

networks:
  default:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 192.168.203.0/24
